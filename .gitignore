# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Secrets
.env

# Data & models (large / regenerated / consented — keep out of git)
/models/
# Served on-device head weights (275 KB JSON, regenerated by linear_train / export_parity_fixtures)
!site/models/
# ...but not the self-hosted 278 MB ONNX embedder (deploy_bootstrap downloads it)
site/models/Xenova/
site/models/qorgan/
# ...nor the Vosk speech models (~106 MB, packaged by deploy_bootstrap for on-device ASR)
site/models/vosk/
# ...nor the pinned Vosklet runtime (2.5 MB wasm, hash-verified by deploy_bootstrap)
site/vendor/
data/raw/
data/synthetic/
# Real partner calls (docs/DATA_INTAKE.md): scrubbed + hashed, still never in git.
data/real/
data/processed/*
!data/processed/manifest.json
data/cache/
*.faiss
*.sqlite
*.db

# OS / editor
.DS_Store
.idea/
.vscode/

# Notebooks
.ipynb_checkpoints/

# Local debug artifacts
error_log.txt
error.txt
Qorgan_transcript.md
node_modules
.playwright-mcp/
# scratch audio from the ASR capture spike; pairs.jsonl itself IS tracked (ADR D39)
data/asr_capture/_wav/
