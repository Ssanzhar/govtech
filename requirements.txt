# Dependencies live in pyproject.toml only. This file exists for tools that expect it.
#   runtime (server, bootstrap, retraining):  pip install -e .
#   + tests:                                  pip install -e ".[dev]"
#   everything (cloud tier, Streamlit harness, research paths):  pip install -e ".[all]"
-e .
